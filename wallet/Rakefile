namespace :tests do
  desc "Run all tests"
  task :all => [:spec,:acceptance]

  desc "Run acceptance tests"
  task :acceptance do
    sh "rspec tests/acceptance/*.rb"
  end

  desc "Run specs"
  task :spec do
    sh "rspec tests/specs/*.rb"
  end
end
